{
  "name": "math-ai-solver",
  "version": "1.1.0",
  "description": "Obsidian plugin: 考研数学 AI 题解",
  "main": "main.js",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "esbuild main.ts --bundle --external:obsidian --format=cjs --platform=browser --target=es2020 --outfile=main.js",
    "dev": "npm run build -- --sourcemap=inline --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.6.0"
  }
}
